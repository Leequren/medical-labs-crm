export enum routes {
    main = "/",
    addShift = "/add-shift",
    staff = "/staff",
    statistics = "/statistics",
    salaries = "/salaries",
    warehouse = "/warehouse",
    archive = "/archive"
}

export enum backendRoutes {

}